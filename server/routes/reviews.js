const express = require("express");
const router = express.Router();

router.get("/", (res, req) => {
  //DB Query goes here
});

router.get("/:id", (res, req) => {
  //DB Query goes here
});

router.post("/", (res, req) => {
  //DB Query goes here
});

module.exports = router;
